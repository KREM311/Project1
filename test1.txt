Самарин Даниил

#include <iostream>//основная библиотека C++
#include <stdlib.h>//криллица
void main()//Основная функция
{//начало функции
	//Предуловие: Вводится значение x, затем программа его обрабатывает и проверяет подходит ли оно под условия	
	//Постусловие: Программа путем подстановки числа x, проверяет условие и если оно верно, считает уравнение
	system("Chcp 1251");//криллица
	int x, y;//Объявление переменных
	printf("Введите значение x: ");//Ввод в консоль
	scanf("%d", &x);//Присвоение введеного в консоль к переменной
	if (x < -10) //Если и условие
	{
		y = 4 * x - 2;//формула
	}
	else if (x < -2) {//Иначе если и условие
		y = 10 * x + 5;//формула
	}
	else {//Иначе если и условие
		y = x + 4;//формула
	}
	printf("y равен %d\n", y);//Вывод y
	system("pause");//Остановка программы
}//конец функции
